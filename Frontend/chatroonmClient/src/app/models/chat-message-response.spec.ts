import { ChatMessageResponse } from './chat-message-response';

describe('ChatMessageResponse', () => {
  it('should create an instance', () => {
    expect(new ChatMessageResponse()).toBeTruthy();
  });
});
