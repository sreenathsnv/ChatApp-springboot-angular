import { DateTimeConverterPipe } from './date-time-converter.pipe';

describe('DateTimeConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new DateTimeConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
